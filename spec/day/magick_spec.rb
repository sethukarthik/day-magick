require "spec_helper"

describe Day::Magick do
  it "has a version number" do
    expect(Day::Magick::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
